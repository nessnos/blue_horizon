"use strict";

var _vitest = require("vitest");
var _context = require("../context.cjs");
(0, _vitest.test)("normalizeOptions", () => {
  (0, _vitest.expect)((0, _context.normalizeOptions)({
    v2SiteKey: "foo"
  })).toEqual({
    v2SiteKey: "foo",
    loaderOptions: {
      params: {
        render: "explicit"
      }
    }
  });
  (0, _vitest.expect)((0, _context.normalizeOptions)({
    v3SiteKey: "bar"
  })).toEqual({
    v3SiteKey: "bar",
    loaderOptions: {
      params: {
        render: "bar"
      }
    }
  });
  (0, _vitest.expect)(() => (0, _context.normalizeOptions)({})).toThrow();
});