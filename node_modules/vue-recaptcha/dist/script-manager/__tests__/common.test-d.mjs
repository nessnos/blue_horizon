import { expectTypeOf, test } from "vitest";
import { normalizeScriptLoaderOptions } from "../common.mjs";
test("ScriptLoaderOptionsInput", () => {
  expectTypeOf(normalizeScriptLoaderOptions).toBeCallableWith({ params: { render: "explicit" } });
});
